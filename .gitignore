# dependencies
node_modules/

# dados locais — nunca commitar
db/*.db
db/*.db-journal
db/*.db-wal
db/*.db-shm
logs/

# saída por vaga (PDFs, receipts, snapshots)
output/

# fontes do perfil (PDFs pessoais) e dados sensíveis — NUNCA para o repo público
profile/sources/
profile/candidate-facts.yaml
profile/master-profile.yaml
profile/tracks.yaml
profile/linkedin-pack.md
.env
.env.local

# dashboard gerado
dashboard/index.html

# sistema
.DS_Store
