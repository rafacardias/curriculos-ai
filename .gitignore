# dependencies
node_modules/

# dados locais — nunca commitar
db/*.db
db/*.db-journal
db/*.db-wal
db/*.db-shm
db/backups/
logs/

# saída por vaga (PDFs, receipts, snapshots)
#
# Os kits contêm o currículo real com nome, e-mail e telefone. O repo é PÚBLICO.
# Isto vale inclusive para "copiar um kit real como fixture de teste": tests/ é
# tão público quanto o resto — é por isso que a sandbox usa a Ana Teste sintética.
# Quando um kit real for evidência de um defeito, ele prova no KNOWN-BUGS.md e uma
# fixture sintética equivalente é quem entra no teste. Ver CLAUDE.md.
output/

# fontes do perfil (PDFs pessoais) e dados sensíveis — NUNCA para o repo público
profile/sources/
profile/candidate-facts.yaml
profile/master-profile.yaml
profile/tracks.yaml
profile/linkedin-pack.md
# currículos-mestre por trilha: são os bullets reais, redigidos a partir dos fatos
profile/masters/
# vocabulário descartado dos mestres — deriva dos fatos reais, mesma regra
profile/synonyms-rejected.md
.env
.env.local

# dashboard gerado
dashboard/index.html

# sandbox descartável da suíte de testes (recriada pelo pretest)
.test-sandbox/

# sistema
.DS_Store
Captura de Tela*.png
